import { Platform } from 'react-native';

const LOCAL_IP = '192.168.1.69'; // YOUR LOCAL IP ADDRESS
const PORT = '8000';
const DEV_URL = `http://${LOCAL_IP}:${PORT}`;

export const API_BASE_URL = DEV_URL;
